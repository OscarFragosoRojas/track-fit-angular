export interface StatCard {
  title: string;
  value: string;
  change: string;
  changeType: 'positive' | 'negative';
  icon: string;
  iconBg: string;
  iconColor: string;
  trendIcon: string;
  trendColor: string;
}
